# 假名練習帳 — 開發流程

這份文件說明這個專案的更新方式。Claude Code 每次在這個 repo 工作時都要先讀這份文件和 `SPEC.md`。

---

## 1. 專案檔案

```
/
├── CLAUDE.md               開發流程（本文件）
├── SPEC.md                 完整規格書，永遠與程式同步
├── index.html              主程式（HTML + CSS + JS 全部內嵌）
├── sw.js                   Service Worker（離線快取）
├── manifest.webmanifest    PWA 設定
├── icon-192.png
├── icon-512.png
└── apple-touch-icon.png
```

目前版本：**v1.7.1**，`sw.js` 快取版本 `kana-v13`。App 名稱為 **KanA**，圖示為紅底白「あ」，全站使用 iPhone 系統字型。

---

## 2. 更新流程

1. **討論**：使用者在 Claude（聊天）討論想要的升級，確認最終做法
2. **更改清單**：Claude 產出一份更改清單 `.md`（格式見第 4 節）
3. **交給 Claude Code**：使用者把更改清單貼給 Claude Code
4. **Claude Code 執行**：依清單修改程式、同步 `SPEC.md`、升級快取版本、自我驗收、commit 並 push 到 GitHub
5. **使用者驗證**：用手機從主畫面開啟 App，重開一兩次讓新版生效後測試

---

## 3. Claude Code 工作守則

- **最小修改**：只改清單列出的地方，不重寫、不重排、不順手重構無關程式碼
- **維持單檔架構**：CSS 與 JS 繼續內嵌在 `index.html`，不新增框架、建置工具或外部依賴
- **每次都要升級快取版本**：`sw.js` 的 `CACHE` 版本號 +1（例如 `kana-v2` → `kana-v3`），否則手機主畫面 App 會一直讀舊版
- **同步規格書**：依清單的「規格書同步」段落更新 `SPEC.md`，並更新 `SPEC.md` 開頭的「目前版本」、本文件第 1 節的版本資訊與第 5 節的版本紀錄
- **保護使用者進度**：不可更改 localStorage key `kana-trainer-v1`；若狀態結構有變動，必須在讀取時相容舊資料並自動轉換
- **例字資料**：`WORD_DATA` 內的中文意思不可出現半形 `,` 或 `;`；新增例字時要確認讀音真的包含目標音節
- **自我驗收**：commit 前逐項檢查清單中的驗收項目，並確認 JS 沒有語法錯誤
- **Commit**：使用清單提供的 commit message，一個版本一個 commit
- **有疑問就停下來問**：清單描述不清楚或與現有程式衝突時，先回報，不要自行猜測

---

## 4. 更改清單格式

每次升級的更改清單都依照以下格式：

```markdown
# 更改清單 v1.x

## 版本
- App 版本：v1.x
- sw.js 快取版本：kana-vN → kana-vN+1

## 變更摘要
一兩句話說明這次改了什麼、為什麼。

## 逐項變更

### 1. 〔變更標題〕
- **目的**：為什麼要改
- **位置**：用函式名稱或區塊定位（例：`answer()`、`/* ---------- 例字 ---------- */` 區塊、`.opt` 樣式），不用行號
- **改法**：具體要新增、修改或刪除什麼
- **注意**：邊界情況、相容性、不要動到的地方

### 2. …

## 規格書同步
- `SPEC.md` 第 x 節：新增／修改哪些內容

## 驗收項目
- [ ] 改完要測的行為 1
- [ ] 改完要測的行為 2
- [ ] 重新整理後原本的進度仍在

## Commit message
`v1.x: 簡短描述`
```

---

## 5. 首次上線（v1.0）

1. 在 GitHub 建立 repo（例如 `kana`）
2. 把這 8 個檔案原封不動上傳到根目錄，作為 v1.0 基準（不要讓 Claude Code 照規格書重寫）
3. Settings › Pages › Source：`main` 分支、`/ (root)`
4. iPhone Safari 開啟 `https://<帳號>.github.io/kana/` → 分享 › 加入主畫面
5. 之後固定從主畫面圖示開啟（主畫面 App 與 Safari 分頁的進度不互通）

---

## 6. 版本紀錄

| 版本 | 快取 | 內容 |
|---|---|---|
| v1.0 | kana-v2 | 初版：假名練習（看字選音、聽音選字、按行選擇）、五十音表、例字卡分音節播放、進度與里程碑、進度碼備份、PWA |
| v1.1 | kana-v5 | 更名為 KanA、圖示改紅底白「あ」、全站改用 iPhone 系統字型；練習頁改為整頁不上下捲動＋題目垂直置中，左滑下一題／右滑回看上一題（唯讀複習不重複計分），未作答不能跳下一題，答對仍自動換題 |
| v1.2 | kana-v6 | 新增「答對：自動下一題／手動」設定（預設自動）；自動模式下「下一題」按鈕加上 1.8 秒倒數進度條，時間到才跳題（原 1.1 秒延長為 1.8 秒）；點例字或切換分頁會中止倒數 |
| v1.3 | kana-v7 | 練習頁鎖定：禁止雙指縮放（viewport `maximum-scale=1, user-scalable=no`）與上下捲動／回彈（`#v-practice` `touch-action:none`、`overscroll-behavior:none`）；左右滑動換題不受影響 |
| v1.4 | kana-v8 | 修正 v1.3 的 `user-scalable=no` 在 iOS 把畫面卡在縮小比例的問題：改回一般 viewport，改用 JS 攔截 `gesturestart` 等手勢＋`touch-action:manipulation` 完全禁止縮放 |
| v1.5 | kana-v9 | 練習頁改為回合制：開始畫面（設定＋題數 10／20／30／50／不限，預設 20）→ 按「開始」答題（上方顯示「第 n／N 題」）→ 做完顯示結算（答對 X／N、正確率）＋「再一回合」。設定移到開始畫面、變更不再立即出題；「不限」按「結束」才結算 |
| v1.6 | kana-v10 | 新增第三種模式「看字打拼音」：顯示假名，用鍵盤打羅馬拼音作答（輸入框＋送出，無四選一），字串比對並接受常見替代拼法（shi/si 等，を o/wo、ん n/nn）；打對綠框、打錯紅框顯示正確拼音；沿用回合制／計分／自動下一題／滑動換題 |
| v1.6.1 | kana-v11 | 修正打字模式叫出鍵盤時題目被推出畫面：打字模式作答時 `body.typing` 改為靠上對齊＋可捲動（`touch-action:auto`、`main` 可捲動），其他模式維持鎖死置中 |
| v1.7 | kana-v12 | 打字模式改用 App 內建 A–Z 鍵盤（iPhone 風），不叫系統鍵盤、畫面鎖定；加入羅馬字→假名即時輸入（IME，ka→か、kya→きゃ），輸入的假名也算對；答題時隱藏底部分頁列、「結束」改藍色按鈕 |
| v1.7.1 | kana-v13 | 內建鍵盤把 ⌫ 退回鍵移到第三排 M 右邊（像 iPhone），「送出」改為整排寬鍵 |
